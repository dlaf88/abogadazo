
<%= simple_form_for(question, html: { class: 'data-fields' }) do |f|  %>

<div class="columns two">
  <fieldset><%= f.input :title,label: false,collection:["Inmigración"], default: "Inmigración" %></fieldset>
  
  <fieldset>
  <%= f.text_field :phone,placeholder: "Ingrese su email" %>
  </fieldset>
</div>
<div class="columns two p__relative">
  <%= f.text_area :description,placeholder:"Cual es su pregunta?" %> 
  <%= f.submit 'Enviar Pregunta'%>
</div>



<% end %>